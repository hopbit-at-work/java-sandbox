	
// declare an integer viariable named 
// size and give it the value 27
int size=27; 

// declare an String variable named "name" 
// and give it the value "Fido"
String name = "Fido";

// declare a Dog object named "myDog" 
// and initialize it with given name and size
Dog myDog = new Dog(name,size); 

// subtract 5 from variable size (it'll result with 22) 
// and settle this value/result to variable x
x=size-5;

// if x is lesser than 3 then, call bark method on 
// myDog object with param/arg 8 given/passed to it
if(x<3) myDog.bark(8); 

while(x>3){ // as long as x i greater than 3 (not equal)
	myDog.play(); // call method call on object myDog
}

// declare variable that is array/list of integer numbers 
// and initialize/fill it with nubers: 2,4,6,8
int[] numList = {2,4,6,8}; 

// print "hello" string/message on standard output
System.out.print("Hello"); 

// print on standard output	
System.out.print("Dog: "+name); 

// declare String variable named "num" and give it value "8"
String num="8"; 

// parse given string as integer number and settle result 
// to declared integer variable named z
int z = Integer.parseInt(num);

try { // start of "try" block
	readTheFile("myFile.txt"); // try to read file named myFile.txt
} catch(FileNotFoundException e) { // catch error/exception if reading failed
	// and write message about error on standard output
	System.out.print("File not found"); 
} // end of "try" block

